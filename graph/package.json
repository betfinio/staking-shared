{
	"name": "staking-shared",
	"license": "UNLICENSED",
	"scripts": {
		"codegen": "graph codegen",
		"build:dev": "graph build --network polygon-amoy",
		"build:prod": "graph build --network matic",
		"build:silent": "graph build",
		"deploy:dev": "graph deploy --node https://api.studio.thegraph.com/deploy/ staking-shared --network polygon-amoy",
		"deploy:prod": "graph deploy --node https://api.studio.thegraph.com/deploy/ staking-shared --network matic"
	},
	"dependencies": {
		"@graphprotocol/graph-cli": "^0.94.0",
		"@graphprotocol/graph-ts": "^0.37.0"
	},
	"devDependencies": {
		"matchstick-as": "0.5.0"
	}
}
